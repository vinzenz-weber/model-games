using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Magneton_Logic : MonoBehaviour
{

    public GameObject magnetPrefab; // Reference to the magnet magnetPrefab
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
